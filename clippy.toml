disallowed-macros = [
    { path = "core::matches", reason = "matches! is for tests only; use an explicit match/if let in production. Exempt tests at the crate root with #![cfg_attr(test, allow(clippy::disallowed_macros))] - module-scoped allows silently do nothing for this lint." },
]
